# Frontend Environment Configuration
# Copy this file to .env.local and update with your values

# Backend Server Configuration
VITE_BACKEND_URL=http://localhost:8000
VITE_BACKEND_PASSWORD=change-me-in-production

# Pairing Client URL (for QR codes)
VITE_PAIRING_URL=http://localhost:3000

# Note: Service credentials are configured in the backend (.env file in backend-python/)
# OR configured via the device pairing system (Settings → Pair Device)